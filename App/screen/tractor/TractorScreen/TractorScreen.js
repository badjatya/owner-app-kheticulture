import React from "react";
import { StyleSheet, Text, View } from "react-native";

const TractorScreen = () => {
  return (
    <View>
      <Text>Tractor Screen</Text>
    </View>
  );
};

export default TractorScreen;

const styles = StyleSheet.create({});
